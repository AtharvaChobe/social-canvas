/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:['pbs.twimg.com',"unsplash.com","images.unsplash.com","upload.wikimedia.org"]
      },
      reactStrictMode: false,
};

export default nextConfig;
